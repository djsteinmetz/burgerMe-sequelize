<p>
	{{#unless devoured}}
	<i class="devour-burger fas fa-utensils" data-id="{{this.id}}"></i>
	{{/unless}}

	<i class="delete-burger fas fa-trash-alt" data-id="{{this.id}}"></i>
	{{burger_name}}: {{#unless devoured}} Ready to eat! <form class="create-form">
		<h3>
			<label for="user">
				<h5>Who is eating this burger?</h5>
			</label>
		</h3>
		<div class="form-group">
			<input type="text" id="user-{{this.id}}" name="name" placeholder="Your Name">
		</div>
	</form>{{/unless}} 
	{{#if devoured}}<br/>Devoured by {{this.Users.0.name}}{{/if}}
</p>